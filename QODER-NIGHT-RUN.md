# 🌙 Qoder 夜间自动开发任务单（无人值守模式）

> 把本文件内容作为第一条指令发给 Qoder（或让它先读本文件），它就会按顺序自主干一整晚。
> 主人明早 9:00 回来验收。

## 开工前必读
1. 先完整阅读本目录下的 `QODER-HANDOFF.md`（项目全景、API 契约、设计规范、铁律）。
2. 严格遵守 HANDOFF 第 10 节铁律，特别是：
   - **禁止 push 到任何 GitHub 远程仓库**，所有改动只提交到本地 git（每完成一个任务 `git add -A && git commit`，commit message 用中文写清楚做了什么）。
   - 禁止改动 `spa-dist/` 手工文件（它是构建产物）。
   - 文案去 AI 化：对外文案不出现"AI 大模型/GPT"等字样，统一叫"私享管家"。
   - 保持黑金视觉风格（#0B0B0D 底 + 金色 #D4AF37 系），不引入花花绿绿的配色。
3. **凡是需要外部账号/密钥/付费/人工确认的事情一律跳过**（如部署 Koyeb、接大模型 API Key、上架、绑卡），在报告里记一笔"待主人处理"即可，不要卡住等待。
4. 遇到构建失败/测试失败：先自己修，修不好就回滚该任务的改动并在报告中说明原因，继续下一个任务。**任何情况下不要让项目处于跑不起来的状态。**

## 今晚任务队列（按顺序做，做完一个 commit 一个）

### T1. 数据可视化面板（P1，优先做）
- 新增「健康趋势」面板组件：体重/心率/睡眠/步数的折线与面积图，30/90 天切换。
- 不引入重型图表库，优先手写 SVG 或轻量方案；配色只用金色系单色渐变。
- 数据源：healthStore 的 records + wearable（本地演示模式也要能看，用已有数据或生成示例数据开关）。

### T2. AI 周报（本地模板版）
- 新增「每周报告」：汇总最近 7 天记录（训练次数、平均睡眠、静息心率变化、步数、体重变化），生成一段中文自然语言总结（模板+规则拼装，不接大模型）。
- 支持导出为文本/复制。

### T3. 移动端体验打磨
- 全站在 ≤ 820px 下逐页走查：修字号过小、按钮过密、横向溢出、表单键盘遮挡问题。
- 底部 tabbar 加当前页高亮过渡动画；页面切换加轻量淡入。

### T4. PWA 增强
- Service Worker 升级版本号，加"发现新版本 → 提示刷新"的更新提示条。
- 加离线兜底页样式（断网时打开显示友好提示而不是白屏）。

### T5. 可穿戴模块增强
- 实时心率会话结束后自动生成"本次会话小结卡片"（时长/均值/峰值/区间分布条）。
- 心率区间（热身/燃脂/有氧/无氧/极限）按年龄可配置（档案里有生日/年龄就自动算 220-年龄）。

### T6. 工程化收尾（时间富余再做）
- 组件懒加载（React.lazy）拆包，减小首屏 bundle。
- 给 healthStore 写基础单元测试（能跑 `npm test` 或用 vitest）。
- 修复构建告警。

## 每完成一个任务必须做
1. `npm run build` 确认构建通过；
2. 本地 git commit（中文 message，格式：`[T1] 健康趋势面板：xxx`）；
3. 在本目录 `NIGHT-REPORT.md` 追加一节：任务名、做了什么、改了哪些文件、自测结果、遗留问题。

## 收工标准（明早 9:00 主人验收）
- `NIGHT-REPORT.md` 里有完整的逐任务报告 + 一段"今晚总结 & 建议下一步"；
- `npm run dev` 能直接跑起来，`npm run build` 无报错；
- git log 里每个任务一个清晰的 commit，没有 push 到远程。
