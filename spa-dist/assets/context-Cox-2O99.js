import{a as e,c as t,o as n}from"./healthStore-BSWtvauT.js";async function r(){let r=``;try{let i=await e();i&&(r+=`【用户健康档案】
`,r+=`姓名: ${i.name||`未填写`}, 性别: ${i.gender||`未知`}, `,r+=`身高: ${i.height||`?`}cm, 体重: ${i.weight||`?`}kg, `,r+=`年龄: ${i.age||`?`}岁`,i.goal&&(r+=`, 目标: ${i.goal}`),r+=`
`);let a=(await n()||[]).slice(0,7);if(a.length>0){r+=`【近7天数据】
`;let e=a.filter(e=>e.sleep_hours>0).map(e=>e.sleep_hours);if(e.length>0){let t=(e.reduce((e,t)=>e+t,0)/e.length).toFixed(1);r+=`平均睡眠: ${t}h\n`}let t=a.filter(e=>e.training_load>0).map(e=>e.training_load);if(t.length>0){let e=Math.round(t.reduce((e,t)=>e+t,0)/t.length);r+=`平均训练负荷: ${e}\n`}let n=a.filter(e=>e.posture_score>0).map(e=>e.posture_score);if(n.length>0){let e=Math.round(n.reduce((e,t)=>e+t,0)/n.length);r+=`平均体态评分: ${e}\n`}}let o=(await t()||[]).slice(0,7);if(o.length>0){r+=`【可穿戴数据(近7天)】
`;let e=o.filter(e=>e.resting_hr>0).map(e=>e.resting_hr);if(e.length>0){let t=Math.round(e.reduce((e,t)=>e+t,0)/e.length);r+=`静息心率: ${t}bpm\n`}let t=o.filter(e=>e.steps>0).map(e=>e.steps);if(t.length>0){let e=Math.round(t.reduce((e,t)=>e+t,0)/t.length);r+=`日均步数: ${e}\n`}let n=o.filter(e=>e.hrv>0).map(e=>e.hrv);if(n.length>0){let e=Math.round(n.reduce((e,t)=>e+t,0)/n.length);r+=`平均HRV: ${e}ms\n`}}}catch{}return r||`（暂无健康数据，用户尚未建档）`}export{r as t};